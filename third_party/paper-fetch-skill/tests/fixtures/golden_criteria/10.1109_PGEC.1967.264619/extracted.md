# IEEE early legacy PDF fallback sample

## Text-Only PDF Fallback

This synthetic Markdown fixture represents the expected shape for an early IEEE article where dynamic HTML is unavailable but a real PDF payload can be converted to text-only Markdown.

Expected public source: `ieee_pdf`.

Expected route: `pdf_fallback`.

Expected content kind: `fulltext`.
